import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Causa Foundation - Positive Impact on Solana Ecosystem',
  description: 'Creating positive impact on the Solana ecosystem and Web3 in general through meaningful projects, community contributions, and charitable initiatives.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
